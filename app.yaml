runtime: nodejs10
env_variables:
  NODE_ENV: "production"
service: default
handlers:
- url: /
  static_files: build/index.html
  upload: build/index.html
  secure: always
- url: /
  static_dir: build
  secure: always
